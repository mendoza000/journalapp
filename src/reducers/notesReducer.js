import {types} from '../types/types.js'

const initialState = {
	notes: [],
	active: null,
	// active: {
	//   id: "o123om123mlsad",
	//	 title: "",
	//   body: ""
	//	 date: ""
	//}
}

export const notesReducer = (state = initialState, action) => {

	switch(action.type){

		
		default:
			return state
	}

}